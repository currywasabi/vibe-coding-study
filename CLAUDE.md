# 소개 페이지 프로젝트

## 개요
개인 소개 페이지. 한 페이지(단일 페이지)로 구성하며, 소개 글 / 관심사 / 링크 섹션 정도로 충분하다.

## 기술 스택
- Next.js
- TypeScript
- Tailwind CSS

## 데이터
- 데이터는 로컬 JSON 파일로 관리한다 (예: `data/profile.json`).
- DB 연동은 지금 하지 않는다. 나중에 별도로 붙일 예정이므로 지금 단계에서 DB 스키마나 연동 코드를 미리 만들지 않는다.

## 하지 말아야 할 것 (지금 단계에서는)
- Vercel 배포 설정/작업 금지
- Supabase 연동 금지
- DB 연동 관련 코드/설정 금지

## 페이지 구성
단일 페이지에 아래 섹션만 포함:
1. 소개 글 (자기소개)
2. 관심사
3. 링크 (예: GitHub, 블로그 등)

불필요한 페이지 라우팅, 인증, 백엔드 API 등은 만들지 않는다.
