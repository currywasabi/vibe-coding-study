# DESIGN

## 이게 뭔가요
박건우의 개인 소개 페이지. 이름, 학과, 진행한 프로젝트, 관심사, 취미, 링크를 한 화면에 보여주는 한 페이지짜리 서비스다. 90년대 컴퓨터 터미널 화면을 흉내 낸 디자인으로, 실제 터미널에서 명령어를 입력해 결과를 조회하는 것처럼 구성했다.

## 화면 구성
페이지는 위에서 아래로 다음 순서로 이어진다.

- **상단 배너**: ASCII 박스 안에 "VIBE-CODING-STUDY TERMINAL" 타이틀과 가짜 로그인 시각 표시
- **소개 (whoami)**: 이름(큰 글씨), 학과, 한 줄 자기소개
- **프로젝트 목록 (ls -la ./projects)**: 진행한 프로젝트를 박스 카드로 나열. 각 카드에는 상태 태그(`[스터디]`, `[현재 폐쇄]`, `[출시 못함]`), 프로젝트 제목, 외부 링크(GitHub/GitLab)가 있고 카드 전체가 클릭 가능한 링크
- **관심사 / 취미 (cat interests.txt / cat hobbies.txt)**: 두 목록을 나란히 배치
- **링크 (cat links.txt)**: GitHub 등 외부 링크 목록
- **하단 커서**: 페이지 맨 아래 깜빡이는 `_` 커서로 "명령 입력 대기 중" 느낌을 마무리

각 섹션 앞에는 `guest@vibe-coding-study:~$ <명령어>` 형태의 가짜 프롬프트가 붙어 있어, 사용자가 터미널에 명령을 쳐서 내용을 조회하는 흐름처럼 읽힌다.

## 색상 / 글꼴
- **배경**: 거의 검정에 가까운 짙은 녹색 (`#0c0f0a`)
- **기본 텍스트**: 형광 녹색 (`#33ff33`), 살짝 빛나는 글로우 효과 적용
- **보조/흐린 텍스트**: 어두운 녹색 (`#1f8a1f`) — 프롬프트의 호스트 이름, 학과 표기 등
- **강조 색**: 앰버(호박색, `#ffb000`) — 프로젝트 상태 태그(`[스터디]` 등)에 사용
- **효과**: 화면 전체에 얇은 스캔라인(가로줄) 오버레이와 미세한 화면 깜빡임 애니메이션으로 CRT 모니터 느낌을 냄
- **글꼴**: 모노스페이스(고정폭) 계열. `D2Coding`, `Nanum Gothic Coding` 등 한글이 지원되는 코딩용 글꼴을 우선 사용하고, 없으면 `Consolas`, `Courier New` 순으로 대체
